// var API_URL = process.env.NODE_ENV === 'development' ?
//     "http://api.foqus.spect8.local:8890" : "https://api.foqus.spect8.no";

const API_URL = 'http://127.0.0.1:5000/';

export const Globals = {
    API_URL,
}